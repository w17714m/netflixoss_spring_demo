

zuul:
  routes:
    saludos:
      path: /saludar/**
      serviceId: SERVICE-SALUDAR
      stripPrefix: false
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8090/eureka/

server:
  port: 8095
spring:
  application:
    name: ZUUL
